name: tests

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Compile Python
        run: python -m compileall -q app tests

      - name: Apply migrations
        env:
          NERISOFT_DATABASE_URL: sqlite:///./ci-nerisoft.db
          NERISOFT_SESSION_SECRET: ci-session-secret-for-nerisoft-tests-2026
        run: python -m alembic upgrade head

      - name: Run tests
        run: python -m pytest -q

      - name: Check JavaScript syntax
        run: |
          node --check app/static/js/app.js
          node --check app/static/js/select.js
          node --check app/static/js/setup.js
